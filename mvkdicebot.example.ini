[MAIN]
# Follow instructions here to get token:
# https://discord.com/developers/docs/getting-started
authtoken = abc1234

# Bot invite:
# https://discord.com/api/oauth2/authorize?client_id=1168083075515826186&permissions=277025442816&scope=bot
# (replace client_id with your bot acct client_id)

# Optional: guild (server) IDs to register slash commands in directly, as a
# comma/whitespace-separated list. This is instant, unlike a global sync (which
# can take up to ~1 hour to propagate). Leave unset/empty to sync globally.
# ("guild" is the Discord API term for a server.)
primary_guilds = 986425163744673874, 910663839220105227

# debug levels:
# 0 -> startup and errors only
# 1 -> small amounts of info
# 2 -> detailed
# 3 -> too much details?
debug = 0
